package stockapi

func GetStockCandles() {
	// finnhubClient := finnhub.NewAPIClient(FinnhubConfiguration).DefaultApi

	// res, response, err := finnhubClient.StockCandles(context.Background()).Symbol("AAPL").Resolution("D").From(time.Now().AddDate(0, 0, -1).Unix()).To(time.Now().Unix()).Execute()

}
